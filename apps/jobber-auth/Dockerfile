# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @jobber/jobber-auth`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t jobber-jobber-auth`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system jobber-jobber-auth && \
          adduser --system -G jobber-jobber-auth jobber-jobber-auth

COPY apps/jobber-auth/dist jobber-jobber-auth/
COPY apps/jobber-auth/package.json jobber-jobber-auth/
RUN chown -R jobber-jobber-auth:jobber-jobber-auth .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix jobber-jobber-auth --omit=dev -f install

CMD [ "node", "jobber-jobber-auth" ]
